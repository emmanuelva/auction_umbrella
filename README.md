# AuctionUmbrella

## Dev commands

### Postgres

Connect to local docker:

`docker container exec -it auction_umbrella_db_1 psql -U postgres`

List databases:

`\l`

Select database:

`\c auction`

Show tables:

`\dt`
